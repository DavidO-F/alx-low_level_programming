#include <stdio.h>

/**
 * main - The main is the entering point of the progarm
 * Return: zero which is the exit point in the function main
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
